apiVersion: 1

providers:
  - name: "RestoHub"
    orgId: 1
    folder: "RestoHub"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
